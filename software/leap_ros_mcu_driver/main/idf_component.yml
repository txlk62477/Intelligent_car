dependencies:
  micro_ros_espidf_component:
    path: ../managed_components/micro_ros_espidf_component
  espressif/led_strip: "^2.5.5"
  espressif/mdns: "~1.1.0"
  espressif/esp_hosted:
    rules:
      - if: "target == esp32p4"
    version: "2.7.0"
  espressif/esp_wifi_remote:
    rules:
      - if: "target == esp32p4"
    version: "0.15.2"
  espressif/lan867x:
    version: "~1.0.0"
    rules:
      - if: "target == esp32"
  idf:
    version: ">=5.3.0"
